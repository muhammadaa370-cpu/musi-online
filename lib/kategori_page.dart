import 'package:flutter/material.dart';

class BeritaKategoriPage extends StatelessWidget {
  const BeritaKategoriPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Berita Kategori")),
      body: Center(child: Text("Halaman Berita Kategori")),
    );
  }
}
