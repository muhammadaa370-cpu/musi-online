import 'package:flutter/material.dart';

class PengingatSholatPage extends StatelessWidget {
  const PengingatSholatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Pengingat Sholat")),
      body: Center(child: Text("Halaman Pengingat Sholat")),
    );
  }
}
